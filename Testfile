You are a senior frontend engineer.

Your task is to recreate the EXACT UI from the provided screenshot with pixel-perfect accuracy.

Requirements:

1. Layout & Spacing
- Maintain PERFECT symmetry between left and right margins.
- The spacing on the left side and right side must be EXACTLY equal.
- The gap between all form fields (vertical spacing) must be consistent and match the screenshot.
- The spacing between sections (top, bottom, and between groups) must also be consistent.
- Follow an 8px grid system unless the screenshot suggests otherwise.

2. Container & Alignment
- Center the entire layout horizontally on the page.
- Use a fixed max-width container (e.g., 960px or based on screenshot proportion).
- Ensure proper padding inside the container.
- All fields should align perfectly in a structured grid.

3. Fields & Components
- Replicate all input fields, dropdowns, labels, and buttons exactly.
- Match:
  - font size
  - font weight
  - border radius
  - input height
  - placeholder styling
- Maintain equal spacing between label and input fields
